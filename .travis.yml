language: bash
sudo: required
services:
  - docker

env:
  global:
    - DOCKER_HOST=tcp://172.16.100.11

before_install:
  - sudo bash -c 'echo DOCKER_OPTS="-H 172.16.100.11" > /etc/default/docker'
  - sudo service docker restart

install:
  - docker-compose build

script:
  - docker-compose up -d
  - sleep 5
  - curl http://localhost/api/apps
